# 仕様書

現時点で決まっている仕様をまとめたものです。仕様を変更・追加したときは、このファイルを直接書き換えたうえで、末尾の「変更履歴」に日付付きで一行追記してください。

## コンセプト

スキンケア・コスメ・サロン・美容費を一元管理する個人向け美容管理アプリ。

## 世界観・見た目の方向性

- 全体UIはシンプルで見やすいことを最優先(健康記録アプリのため)
- 配色は淡い水色×白を基調。上品でキラキラした「舞踏会・ブライダル」寄りの雰囲気を、装飾よりも配色・見出し・アクセントで表現する
- アイコンは当面「英語表記のいい感じのフォント」による文字アイコンで対応し、あとから画像(ぬいぐるみ・もちもち質感のイラスト等)に差し替えられる構造にしておく
- 主要導線はタブ切り替え(ホーム/肌管理/コスメ/サロン/美容費)

## 日付の扱い(共通ルール)

- データとして保存する日付はすべて `YYYY-MM-DD` 形式に統一する(年をまたいでも壊れないようにするため)
- 画面表示のときだけ `yyyy/MM/DD(曜日)` 形式に変換する。例: `2026/08/22(土)`

---

## ① ホーム画面 [v1]

- 日付表示のみ(あおり文言は不要)
- 皮膚科レベル(重症度が最も高い)の部位がある場合はアラート表示(タップで肌管理へ遷移)
- 次回サロン推奨日(最も近いもの) ※サロン機能はPhase3以降のため、Phase3実装後に追加
- 各セクションへのショートカット(肌管理/コスメ/サロン/美容費)

## ② 肌管理(スキン+お薬管理) [v1]

### 肌記録

- 全身を部位分けして管理: 顔・首・体幹・右腕・左腕・右手・左手・右脚・左脚・右足・左足
- 部位ごとに重症度を4段階(0〜3、数字が大きいほど悪い)で日付付きで記録する
  - 0: 🟢 なんか元気!
  - 1: 🟡 まあまあ
  - 2: 🟠 かゆいけどまだ行ける
  - 3: 🔴 皮膚科レベル(かなり辛い)
- 部位タップで詳細画面(最新状態+記録履歴+当日の記録ボタン)を表示
- 履歴から通院周期の目安を把握できるようにする

### お薬管理

- 薬ごとに管理: 名前/用法(飲み薬 or 塗り薬、1日の使用回数、頓服かどうか)/もらった本数・日付/現在の残り本数/使用履歴
- 「今日使った」ボタンで使用日を記録する(この記録では在庫は減らさない。本数は「使い切ったとき」に手動で減らす。軟膏1本で何十回も使うため、使用のたびに自動で1本減らす方式は取らない)
- 使用記録には任意でその日の効き目(「効く」「きいてる気がする」「保湿かな」)と使った部位を記録できる(時期による効き目の変化を後から追える)
- 「受診サマリー」表示: すべての薬の効き目・受け取り日・残り本数を一覧化し、診察時にそのまま見せられる形にする

### データ構造(v1)

```jsonc
{
  "skinLogs": [
    { "date": "2026-08-22", "part": "face", "severity": 2 }
  ],
  "medicines": [
    {
      "id": "med_001",
      "name": "リンデロン-VG軟膏",
      "form": "topical",        // topical(塗り薬) | oral(飲み薬)
      "timesPerDay": 2,
      "asNeeded": false,        // 頓服ならtrue
      "receivedAt": "2026-08-01",
      "receivedCount": 5,
      "stock": 3,
      "discontinuedAt": null    // 中止した場合はその日付
    }
  ],
  "medicineLogs": [
    {
      "date": "2026-08-22",
      "medicineId": "med_001",
      "parts": ["face"],       // 任意
      "effect": "good"         // 任意。good | maybe | moist
    }
  ]
}
```

---

## ③ コスメ在庫管理 [Phase2]

- 表示切替: 顔パーツタップ式 ⇄ リスト表示(トグルで切替)
- 顔パーツ: 肌(ベース)/眉/目元/頬/口元
- 各アイテム情報: 名前/ブランド/残量%/開封日/交換推奨サイクル(月数)。経過月数・交換目安は保存せず表示時に計算する
- 交換目安表示(すぐ交換目安/そろそろ/まだ大丈夫)はON/OFFトグルで非表示にできる
- 残量が少ないアイテムには「残りわずか」バッジ
- 各アイテムからAmazon検索リンク(ブランド名+商品名)。残量が少ない場合は強調ボタン表示

## ④ サロン・施術履歴+カレンダー [Phase3]

- 種類問わず全部まとめて管理: 脱毛/まつエク/ネイル/美容院/エステ 等
- 施術履歴: 種類/店舗名/日付/金額
- 次回オススメ日は種類ごとに算出する。**デフォルトの周期を持ちつつ、その種類の履歴が2件以上たまったら実測の平均間隔で上書きする**方式
- オススメ日までの残日数を表示(近いほど強調色)
- 種類ごとに通知ON/OFFトグル

## ⑤ 美容費トラッキング [Phase4]

- デフォルトで金額をぼかし表示(プライバシー配慮)。タップで解除
- 金額を隠した状態でも状態を色だけで把握できるようにする(緑=セーフ/黄=注意/赤=やばい)
- **月ごとに予算を設定**し、その月の支出が予算を超えたら赤系の色で警告。余った分は翌月の予算に繰り越し加算する
- カテゴリ別内訳(スキンケア・薬/コスメ/サロン)も同様にぼかし対応。金額は各機能の実データ(薬の受け取り記録・コスメ購入額・サロン施術額)から集計し、手入力の二重管理はしない

---

## 全体の非機能要件

- 記録・提案は事実ベースで表示し、過剰な煽り文言・励まし文言は入れない
- 薬・かゆみの記録はあくまで「記録ツール」であり、診断や治療方針の判断はしない(病院での提示用)
- データは端末内保存のみ。サーバーには送信しない。JSONエクスポート/インポートでバックアップ・機種変更に対応する

---

## 変更履歴

- 2026-08-22: 初版作成。v1のスコープをホーム/肌管理/お薬管理に決定。日付形式・重症度の向き・薬の在庫管理方式・次回オススメ日ロジック・予算の色分けルールを確定
